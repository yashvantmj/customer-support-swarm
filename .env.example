# Example environment file for customer-support-swarm
# Rename this file to `.env` and replace placeholders with real values.

# Groq / LangChain API key (required)
GROQ_API_KEY=REPLACE_WITH_YOUR_GROQ_API_KEY

# Chat model name and temperature (optional)
MODEL_NAME=llama-3.1-70b-instant
TEMPERATURE=0.2

# Helpful debug flags
VERBOSE=false
